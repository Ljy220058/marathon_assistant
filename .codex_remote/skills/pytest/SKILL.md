---
name: pytest
description: Lightweight pytest alias for Kanban tasks that request the short skill name "pytest".
---

# Pytest

Run the smallest relevant pytest target first, inspect failures before editing, and rerun the affected tests after changes. Keep fixtures isolated and avoid depending on shared mutable state.
