# Submission Go / No-Go Report

Date: 2026-05-13

Target: RuleML+RR 2026 Rule Challenge

## Current Decision

**Current status: Go for final local verification; No-Go for final upload until public branch push and clean-clone reproduction are complete.**

The package now has the expected Rule Challenge shape: CEURART manuscript, M-EXRxBench v0.4, system/gold split, rule specifications, deterministic runners, baselines, evaluator, validator, trace viewer, artifact checklist, public GitHub target, and license policy.

## Ready Items

- Track locked to Rule Challenge as a hybrid Challenge Proposal + Challenge Solution.
- Venue/date corrected to RuleML+RR 2026, Vilnius, Lithuania, 24-26 August 2026.
- Public release target recorded: https://github.com/Ljy220058/m-exrxbench
- License policy recorded: code MIT; synthetic benchmark data and documentation CC BY 4.0.
- License files, requirements note, release `.gitignore`, manifest, and reviewer quick path are present.
- Default M-EXRxBench v0.4 / 500 synthetic benchmark and evaluator-only gold split are present.
- Deterministic demo, benchmark, baselines, evaluator, summarizer, validator, and trace viewer are present.
- Manuscript states that metrics are synthetic benchmark rule-compliance signals, not clinical validation.
- Workspace boundary is documented; unrelated manuscript sources and outputs remain out of scope.

## Remaining Upload Blockers

| Gate | Required Before Upload | Current Status |
|---|---|---|
| Local artifact validation | `artifact_validation_ok` after final edits | pending final run |
| Reproducibility | `m_exrx_reproducibility_ok` after final edits | pending final run |
| Paper compile | current source produces 8-15 page PDF | pending final run |
| Public release | branch containing artifact is pushed to GitHub | pending user-controlled push |
| Clean clone | public repo clone can run reviewer path | pending after push |

## Claim Discipline

Allowed claim:

- The artifact defines a reproducible synthetic Rule Challenge benchmark and deterministic rule-governed reference solver.
- The reported metrics are current-evaluator rule-compliance results on M-EXRxBench v0.4.
- Reviewer/reproduction outputs are keyed to `artifacts\demo_runs\full_rule_governed_v04.json`.

Disallowed claim:

- Clinical validation.
- Real-world deployment safety.
- Independent generalization performance.
- Superiority over externally run LLM/RAG systems.
- Improved athlete outcomes.

## Final Go Commands

Run from artifact root:

```powershell
conda run -n torch2.5.1 python demo\validate_artifacts.py
powershell -ExecutionPolicy Bypass -File reproducibility\run_all.ps1
conda run -n torch2.5.1 python demo\validate_artifacts.py
```

Expected reviewer output regenerated by `run_all.ps1`:

```text
artifacts\demo_runs\full_rule_governed_v04.json
```

Run from `paper/`:

```powershell
latexmk -C main.tex
latexmk -xelatex -interaction=nonstopmode -halt-on-error main.tex
```

Final text hygiene scan: run `demo/validate_artifacts.py` and a repository-level
secret scan before upload.

## Decision Rule

Submit to Rule Challenge only after all final gates pass and the public GitHub branch is available to reviewers.
