{
  "name": "marathon-assistant-astro",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev --host 0.0.0.0 --port 4321",
    "dev:ascii": "start_ascii.cmd",
    "build": "astro build",
    "check": "astro check",
    "lint": "eslint src/scripts scripts tests",
    "preview": "astro preview --host 0.0.0.0 --port 4321",
    "smoke": "npm run smoke:workspace",
    "smoke:workspace": "node scripts/smoke-workspace.mjs",
    "test": "node --test"
  },
  "dependencies": {
    "astro": "^6.3.1"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.9",
    "eslint": "^10.4.1",
    "playwright": "^1.60.0",
    "typescript": "^6.0.3"
  }
}
