-- Baseline marker for feedback audit fields.
-- Legacy databases are expanded by the compatibility guard in database.py:
-- training_event_feedback.risk_gate_json and protocol_recheck_json.
SELECT '0002_feedback_audit_fields';
